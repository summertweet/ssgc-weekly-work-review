// 复制为 supabase-config.js，并填写 Supabase 项目配置。
// 这里只能放 anon public key，绝不能放 service_role key。
window.SUPABASE_CONFIG = {
  url: 'https://YOUR_PROJECT.supabase.co',
  anonKey: 'YOUR_SUPABASE_ANON_KEY'
};
